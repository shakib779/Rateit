<%= simple_form_for([@book, @book.bookreps.build])  do |f| %>
	<%= f.input :comment, label:"Comment" %>
	<%= f.submit ( f.object.new_record? ? "Comment" : "Update"), :class => "btn-custom2" %>
<% end %>